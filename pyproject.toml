[project]
name = "paperstack"
version = "0.1.0"
description = "Production-grade retrieval system for scientific documents"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
pythonpath = ["src"]
